export interface PredictionHistory {
  id: number;
  age: number;
  education: string;
  occupation: string;
  maritalStatus: string;
  hoursPerWeek: number;
  prediction: string;
  confidence: number;
  timestamp: string;
}